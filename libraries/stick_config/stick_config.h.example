#pragma once

// Stick OS device configuration.
// Copy this to stick_config.h and fill in your values.
// stick_config.h is gitignored — never commit credentials.

// Server host for API, app store, OTA, and WiFi sync.
static const char* kStickServerHost = "your-server.example.com";
static const int   kStickServerPort = 443;  // HTTPS

// Temporary WiFi stubs — will be replaced by NVS-based WiFi management
struct WiFiNetwork { const char* ssid; const char* password; };
static const WiFiNetwork kWiFiNetworks[] = { { "your-ssid", "your-password" } };
static const size_t kWiFiNetworkCount = 1;
